using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VolunteerState : MonoBehaviour
{
    public bool amIEmployed;   // have I been purchased

    public bool amIAssigned;  // have I got a job to do
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
